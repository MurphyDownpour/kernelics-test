import React from 'react';
import './index.css';

export const Header = () => (
  <div className="logo-container">
    <h1 className="logo">Employees</h1>
    <button className="logout-button">Log Out</button>
  </div>
);
